[package]
name = "mermaid-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
mermaid-core = { path = "../mermaid-core", features = ["wasm"] }
wasm-bindgen = "0.2"
